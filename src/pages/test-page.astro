---
import Layout from '~/layouts/PageLayout.astro';

const logos = [
  { name: 'Logo 1', src: 'https://placehold.co/180x48/111111/FFFFFF?text=Logo+1' },
  { name: 'Logo 2', src: 'https://placehold.co/180x48/1f2933/FFFFFF?text=Logo+2' },
  { name: 'Logo 3', src: 'https://placehold.co/180x48/111827/FFFFFF?text=Logo+3' },
  { name: 'Logo 4', src: 'https://placehold.co/180x48/020617/FFFFFF?text=Logo+4' },
  { name: 'Logo 5', src: 'https://placehold.co/180x48/0b1120/FFFFFF?text=Logo+5' },
  { name: 'Logo 6', src: 'https://placehold.co/180x48/020617/FFFFFF?text=Logo+6' },
];

const metadata = {
  title: 'Logo Layout Playground — The Second Design',
  description: 'Testing different logo strip layouts for the homepage.',
};
---

<Layout metadata={metadata}>
  <main class="bg-white dark:bg-slate-950 text-slate-900 dark:text-slate-50">
    <section class="py-16 border-b border-slate-200 dark:border-slate-800">
      <div class="container mx-auto max-w-5xl space-y-8 text-center">
        <h1 class="text-3xl font-bold tracking-tight">
          Logo Layout Playground
        </h1>
        <p class="text-slate-600 dark:text-slate-300 text-base leading-relaxed max-w-2xl mx-auto">
          These are dummy logos pulled from an online placeholder service so you can decide which layout
          feels right before wiring in the actual brand assets.
        </p>
      </div>
    </section>

    <!-- Layout 1: Simple centered strip ******************************** -->
    <section class="py-16 bg-slate-50 dark:bg-slate-950">
      <div class="container mx-auto max-w-6xl space-y-10 text-center">
        <div class="space-y-3">
          <p class="text-xs font-semibold tracking-[0.25em] uppercase text-slate-500 dark:text-slate-400">
            Layout 1
          </p>
          <h2 class="text-2xl font-semibold tracking-tight">
            Simple centered strip
          </h2>
          <p class="text-sm text-slate-600 dark:text-slate-300 max-w-2xl mx-auto">
            Clean, minimal strip. Best when you just want a subtle row of credibility logos
            without stealing focus from the hero.
          </p>
        </div>

        <div class="flex flex-wrap items-center justify-center gap-x-12 gap-y-6 opacity-80">
          {
            logos.map((logo) => (
              <img
                src={logo.src}
                alt={logo.name}
                loading="lazy"
                class="h-10 w-auto object-contain dark:brightness-125"
              />
            ))
          }
        </div>
      </div>
    </section>

    <!-- Layout 2: Carded grid ****************************************** -->
    <section class="py-16 border-y border-slate-200 bg-white dark:bg-slate-950 dark:border-slate-800">
      <div class="container mx-auto max-w-6xl space-y-10">
        <div class="text-center space-y-3">
          <p class="text-xs font-semibold tracking-[0.25em] uppercase text-slate-500 dark:text-slate-400">
            Layout 2
          </p>
          <h2 class="text-2xl font-semibold tracking-tight">
            Carded grid with subtle depth
          </h2>
          <p class="text-sm text-slate-600 dark:text-slate-300 max-w-2xl mx-auto">
            Each logo sits inside a soft card. Feels more “productized” and works well if you
            want to group or label logos later (e.g. consulting, RWR, products).
          </p>
        </div>

        <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {
            logos.map((logo) => (
              <div
                class="flex items-center justify-center rounded-2xl border
                       border-slate-200 bg-slate-50/80 shadow-sm
                       dark:border-slate-800 dark:bg-slate-900/70 dark:shadow-black/40
                       px-6 py-6"
              >
                <img
                  src={logo.src}
                  alt={logo.name}
                  loading="lazy"
                  class="h-10 w-auto object-contain dark:brightness-125"
                />
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Layout 3: Horizontal marquee / scroll *************************** -->
    <section class="py-16 bg-slate-50 dark:bg-slate-950">
      <div class="container mx-auto max-w-6xl space-y-10">
        <div class="text-center space-y-3">
          <p class="text-xs font-semibold tracking-[0.25em] uppercase text-slate-500 dark:text-slate-400">
            Layout 3
          </p>
          <h2 class="text-2xl font-semibold tracking-tight">
            Subtle scrolling strip
          </h2>
          <p class="text-sm text-slate-600 dark:text-slate-300 max-w-2xl mx-auto">
            Faux-marquee using pure CSS. Great when you want motion without a heavy JS carousel.
            Try narrowing your browser to see the scroll behaviour.
          </p>
        </div>

        <div class="relative overflow-hidden">
          <div
            class="flex gap-12 animate-[logo-marquee_30s_linear_infinite]
                   hover:[animation-play-state:paused]"
          >
            {
              [...logos, ...logos].map((logo, index) => (
                <div class="flex items-center justify-center flex-none" style="min-width: 160px" >
                  <img
                    src={logo.src}
                    alt={logo.name}
                    loading="lazy"
                    class="h-10 w-auto object-contain dark:brightness-125"
                  />
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>
  </main>

  <style>
    @keyframes logo-marquee {
      0% {
        transform: translateX(0%);
      }
      100% {
        transform: translateX(-50%);
      }
    }
  </style>
</Layout>
